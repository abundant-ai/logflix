# GitHub Repository Configuration
# These are optional - if not set, the gh CLI will use the current directory's repository
GITHUB_REPO_OWNER=abundant-ai
GITHUB_REPO_NAME=tbench-hammer
GITHUB_WORKFLOW_FILE=test-tasks.yaml

# The gh CLI must be authenticated via SSH
# Run: gh auth login
# Choose SSH as the authentication method