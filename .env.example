# ==================================
# LogFlix Environment Configuration
# ==================================

# --------------------------
# Required Configuration
# --------------------------

# Clerk Authentication
# Get these from https://dashboard.clerk.com
VITE_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_KEY
CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_KEY
CLERK_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_KEY

# Express Session Secret
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-here

# Logging Level
LOG_LEVEL=info

# GitHub Sync Configuration
# How often to sync repository access from GitHub (in hours)
GITHUB_SYNC_INTERVAL_HOURS=24

# --------------------------
# Optional Configuration
# --------------------------

# Database (only needed if using database features)
# DATABASE_URL=postgresql://user:password@localhost:5432/logflix

# Clerk Webhooks (only if implementing webhook endpoints)
# CLERK_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_KEY

# Custom Clerk URLs (defaults work for most cases)
# VITE_CLERK_SIGN_IN_URL=/sign-in
# VITE_CLERK_SIGN_UP_URL=/sign-up
# VITE_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
# VITE_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/
# VITE_CLERK_SIGN_OUT_REDIRECT_URL=/sign-in

# Custom API URL (only if API is on different domain)
# VITE_API_URL=https://api.yourdomain.com

# Clerk proxy (only for custom domain proxy setups)
# VITE_CLERK_PROXY_URL=https://yourdomain.com/__clerk

# CORS origins (only if implementing custom CORS)
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
